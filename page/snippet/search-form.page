---
title: Search Form
description: Basic search form.
...

~~~ .php
<form action="<?php echo $site->is === 'page' ? Path::D($url->current) : $url->current; ?>" method="get">
  <?php echo Form::text($config->q, $site->search) . ' ' . Form::submit($language->search); ?>
</form>
~~~