---
@version: [current]
@description: Remove array value recursively using dot notation.
@lot:
- array &$data, string $access, string $join = '.'
@example: |
  // Remove `$_GET['foo']['bar']['baz']` value
  let($_GET, 'foo.bar.baz');
...