---
@version: [current]
@description: Set array value recursively using dot notation.
@lot:
- array &$data, string $access, $value = null, string $join = '.'
@example: |
  // Set `$_GET['foo']['bar']['baz']` value to `true`
  set($_GET, 'foo.bar.baz', true);
...