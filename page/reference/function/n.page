---
@version: [2, current]
@description: String normalization.
@lot:
- string $x = null, string $t = '    '
...

This function does the following tasks:

 - Removes multiple UTF-8 BOM sequences
 - Replaces `\t` characters to `$t`
 - Normalizes line-break characters to `\n` character.