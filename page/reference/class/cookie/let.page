---
@version: 2.2
@description: Remove a cookie.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all cookies
  Cookie::let();
  // Remove `foo` cookie
  Cookie::let('foo');
  // Remove `foo` and `bar` cookie
  Cookie::let(['foo', 'bar']);
...
