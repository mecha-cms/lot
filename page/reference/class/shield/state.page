---
@version: [2, current]
@description: Get shield state data.
@lot:
- string $id
- string $id, string $key
- string $id, array $alt
@static: 1
@example: |
  $state = Shield::state('document');

  $A = Shield::state('document', 'page');
  $B = Shield::state('document', 'query');

  // If the second argument is an array, then it will become the
  // alternate values of the `$C` output if some data are not available
  $C = Shield::state('document', [
      'query' => ['foo', 'bar', 'baz'] // add `query` property to `$C` if missing
  ]);
...

By default, this method will try to load `config.php` file stored in the shield’s `state` folder. Since version `2.2.0`, if you have custom state data included in the `state` folder, you can get its value by including the state name after the shield ID separated by a colon:

~~~ .php
// Load `widget.php`
$widget_state = Shield::state('document:widget');
~~~