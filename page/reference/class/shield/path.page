---
@version: [2, current]
@description: Generate all possible shield’s file path to be tested by [link:../file/exist].
@lot: $in
@static: 1
@example: |
  // Search for files relative to the current shield
  // folder with extension `html` or `php` based on
  // the given name. Otherwise, return `null`
  $A = Shield::path('page');
  $B = Shield::path('foo');

  // Search for files relative to the current shield
  // folder with extension `html` or `php` based on
  // the given _namespace_. Otherwise, return `null`
  $C = Shield::path('page/about/history')
@result: |
  $A = '.\lot\shield\<id>\page.html'; // or `.\lot\shield\<id>\page.php`
  $B = false;

  // Can be
  $C = '.\lot\shield\<id>\page.about.history.html';
  // or
  $C = '.\lot\shield\<id>\page.about.history.php';
  // or
  $C = '.\lot\shield\<id>\page\about\history.html';
  // or
  $C = '.\lot\shield\<id>\page\about\history.php';
  // or
  $C = '.\lot\shield\<id>\page\about.html';
  // or
  $C = '.\lot\shield\<id>\page\about.php';
  // or
  $C = '.\lot\shield\<id>\page.html';
  // or
  $C = '.\lot\shield\<id>\page.php';
  // or
  $C = false;
@result-note: `<id>` is based on `Shield::$config['id']` and file extension mapping is based on `Shield::$config['x']`.
...