---
title: `$h_t_t_p::status()`
description: Set HTTP status by code.
...

~~~ .php.xmp
HTTP::status($i = null);
~~~

List of HTTP status code:

Code | Description
---- | -----------
[[e]]foreach (HTTP::$message as $k => $v) {
    echo '`' . $k . '` | ' . ($v ? $v : '`' . s($v) . '`') . N;
}[[/e]]

**Example:**

Get current HTTP response status:

~~~ .php
echo HTTP::status();
~~~

Set HTTP response status:

~~~ .php
HTTP::status(404); // “Not Found”
~~~