---
@version: [2, current]
@description: Return the evaluated [_superglobals_](http://php.net/manual/en/language.variables.superglobals.php) variable values.
@lot: string $kin, array $lot = []
@static: 1
@example: |
  $cookie = HTTP::cookie(); // `$_COOKIE`
  $env = HTTP::env(); // `$_ENV`
  $files = HTTP::files(); // `$_FILES`
  $get = HTTP::get(); // `$_GET`
  $post = HTTP::post(); // `$_POST`
  $request = HTTP::request(); // `$_REQUEST`
  $server = HTTP::server(); // `$_SERVER`
  $session = HTTP::session(); // `$_SESSION`
...

> **Note:** Values include `0`, `'0'`, and `' '` are considered not empty. So, returning a data contains these kind of value will not trigger the default value.