---
@version: 2
@description: Remove global state item.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all items
  State::let();
  // Remove `foo` item
  State::let('foo');
  // Remove `foo` and `bar` item
  State::let(['foo', 'bar']);
...
