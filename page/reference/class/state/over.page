---
@version: [current]
@description: Set item or items only when it is not set.
@lot:
- string $key, $value
- array $values
@static: 1
@example: |
  // Set a `test` item with value `#` if not set
  State::over('test', '#');
  // Set a `test` item with value `['foo' => '#']` if not set
  State::over('test.foo', '#');
  // Set `foo` and `baz` item if not set
  State::over([
      'foo' => 'bar',
      'baz' => 'qux'
  ]);
...