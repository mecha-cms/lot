---
title: `Plugin`
description: Working with plugins.
...

> **Info:** This class is located in `lot\extend\plugin\engine\kernel` folder.

[connect:../extension/plugin]

[[e]]
echo "### Methods\n";
foreach (glob(PAGE . DS . $url->path . DS . '*.page') as $v) {
    $n = basename($v, '.page');
    if ($n === '$' || $n === basename(dirname($v))) continue;
    $v = Page::open($v);
    echo "\n - [" . $v->title . '](' . $v->url . ')';
}
[[/e]]