---
@version: [current]
@description: Get current hook.
@lot:
- void
- string $id
@static: 1
@example: |
  // Get current hook
  test(Hook::is());
  // Check if current hook container name was `set`
  if (Hook::is('set')) { … }
...