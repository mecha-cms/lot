---
@version: [2, current]
@description: Get a hook.
@lot:
  - void
  - string $id = null, $fail = false
@static: 1
@example: |
  // Get all of the attached hook(s)
  test(Hook::get());
  // Get all hook(s) attached to `head.content`
  test(Hook::get('head.content'));
...