---
@version: [current]
@description: Render layout from file.
@parent: genome
...

### Configuration

~~~ .php
Layout::$state = [
    'path' => LOT . DS . 'layout',
    // List of alternative file extensions used by the layout files.
    // Layout with `html` extension is a priority for best performance.
    // To change the priority order, re-order this file extensions placement.
    'x' => ['html', 'php']
];
~~~