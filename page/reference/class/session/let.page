---
@version: 2
@description: Remove a session.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all sessions
  Session::let();
  // Remove `foo` session
  Session::let('foo');
  // Remove `foo` and `bar` session
  Session::let(['foo', 'bar']);
...
