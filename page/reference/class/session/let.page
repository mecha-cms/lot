---
@version: [2, current]
@description: Remove a session.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all session(s)
  Session::reset();
  // Remove `foo` session
  Session::reset('foo');
  // Remove `foo` and `bar` session
  Session::reset(['foo', 'bar']);
...