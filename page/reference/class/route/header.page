---
@version: [current]
@description: Set response headers.
@example: |
  Route::set('foo/bar', 200, function() {
      $this->header('X-Foo', 1);
      $this->header('X-Bar', 2);
      $this->header([
          'X-Baz' => 3,
          'X-Qux' => 4
      ]);
      echo 'Success 200: Okay.';
      exit;
  });
...