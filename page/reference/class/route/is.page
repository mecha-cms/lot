---
@version: [2, current]
@description: Check whether current URL path is matched with the given route pattern.
@lot:
- string $id
- array $id
@static: 1
@example: |
  if (Route::is('blog/:slug')) { … }
@example-note: This method will return the route data as class instance parsed from the current URL path or `false`.
...