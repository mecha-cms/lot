---
@version: [2, current]
@description: Check the existence of a specific route pattern.
@lot:
- void
- string $id
@static: 1
@example: |
  test(
      Route::get('blog'),
      Route::get('blog/(\d+)'),
      Route::get('foo/bar')
  );
...