---
@version: 2
@description: Check the existence of a specific route pattern.
@lot:
- void
- string $id
@static: 1
@example: |
  test(
      Route::get(),
      Route::get('blog'),
      Route::get('blog/:slug'),
      Route::get('foo/bar')
  );
...
