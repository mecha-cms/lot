---
@version: [2, current]
@description: Remove a route.
@lot:
- void
- string $id
- array $id
@static: 1
@example: |
  // Remove all route(s)
  Route::reset();
  // Remove `blog/(\d+)` route
  Route::reset('blog/(\d+)');
  // Remove `blog` and `blog/(\d+)` route
  Route::reset(['blog', 'blog/(\d+)']);
...