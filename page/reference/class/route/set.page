---
@version: [2, current]
@description: Set a route.
@lot:
- string $id, callable $fn = null, float $stack = 10
- array $id, callable $fn = null, float $stack = 10
@static: 1
@example: |
  Route::set('blog/(\d+)', function($step = 1) {
      // In this scope, `$step` variable will return `4` in `‌http://127.0.0.1‌/blog/4`
      // and will return `1` if we are in `‌http://127.0.0.1‌/blog`
  });
...