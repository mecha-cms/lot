---
@version: [current]
@description: Set or get custom actions before specific route pattern execution.
@lot:
- string $id
- array $id
- string $id, callable $fn, float $stack = 10
- array $id, callable $fn, float $stack = 10
@static: 1
@example: |
  // Get all custom actions added before `blog/(\d+)` route execution
  test(Route::lot('blog/(\d+)'));

  // Set custom action before `blog/(\d+)` route execution
  Route::lot('blog/(\d+)', function($step = 1) { … });
...