---
@version: [2, current]
@description: Execute the added routes.
@lot:
- string $id
- string $id, array $lot
@static: 1
@example: |
  // Execute specific route
  Route::fire('blog');

  // Execute specific route with pre-defined value
  Route::fire('blog/([^/]+)/(\d+)', ['archive', 4]);
...