---
@version: [2, current]
@description: Define custom date pattern.
@static: 1
@lot:
- string $kin, string $pattern
- string $kin, callable $fn
@parent: genome
@example: |
  Date::_('en', '%A, %B %d, %Y');

  $date = new Date('[[e]]echo date('Y-m-d H:i:s');[[/e]]');

  echo $date->en;
@example-description: 'The `_` method of the `Date` class can accept string value that represent [date pattern][link:f]. This can be used as a way to create a method that simply return a formatted date. Other than that, the results will inherit to [`Genome::_()`][link:../genome#reference:plug].'
...