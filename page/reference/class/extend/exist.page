---
@version: [2, current]
@description: Return true if extension is active (i.e. has `index.php` file).
@lot: string $id, $active = true
@static: 1
@example: |
  if (Extend::exist('minify')) {
      // Extension `minify` does exist and active …
  } else if (Extend::exist('minify', false)) {
      // Folder `minify` does exist …
  }
...