---
@version: [2, current]
@description: Return the stored extension states if any, otherwise, return `$fail`.
@lot:
  - string $id, string $key = null, $fail = false
  - string $id, array $alt = []
@static: 1
@example: |
  $state = Extend::state('minify');

  $A = Extend::state('minify', 'html');
  $B = Extend::state('minify', 'json');

  // If the second argument is an array, then it will become the
  // alternate values of the `$C` output if some data are not available
  $C = Extend::state('minify', [
      'json' => [] // add `json` property to `$C` if missing
  ]);
...

By default, this method will try to load `config.php` file stored in the extension’s `state` folder. Since version `2.2.0`, if you have custom state data included in the `state` folder, you can get its value by including the state name after the extension ID separated by a colon:

~~~ .php
$editor_state = Extend::state('c-k-editor:editor');
~~~