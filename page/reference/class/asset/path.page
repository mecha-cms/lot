---
title: `$asset::path()`
description: Generate full private file path of the `$path`.
...

~~~ .php.xmp
Asset::path($path, $fail = false);
~~~

**Example:**

~~~ .php
$path = Asset::path('.\lot\asset\css\style.css');
~~~

> **Note:** Setting external URL to the `$path` parameter will return to `$fail`.