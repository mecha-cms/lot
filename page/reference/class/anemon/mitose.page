---
@version: [current]
@description: Create new instance without reference it to a variable.
@example: |
  $anemon = new Anemon(['foo', 'bar', 'baz']);

  $A = $anemon->mitose()->map(function($v) {
      return 'a:' . $v;
  });
  $B = $anemon->mitose()->map(function($v) {
      return 'b:' . $v;
  });

  echo $A;
  echo $B;

  echo $A->first . ', ' . $B->first . ', ' . $anemon->first;

  test($A->parent, $anemon->parent);
@result: |
  'a:foo, a:bar, a:baz'
  'b:foo, b:bar, b:baz'

  'a:foo, b:foo, foo'

  [
      Anemon::__set_state([ … ]),
      null
  ]
...

The cloned anemon instances will have a reference to their original instance in their `parent` property.