---
@version: 2
@description: Shortcut for calling methods with empty arguments.
@lot:
- string $key
@example: |
  Anemon::from([1, 2, 3, 4, 5])->count;
  Anemon::from([1, 2, 3, 4, 5])->next->current;
  // … is the same as …
  Anemon::from([1, 2, 3, 4, 5])->count();
  Anemon::from([1, 2, 3, 4, 5])->next()->current();
...
