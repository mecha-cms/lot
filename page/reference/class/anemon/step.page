---
@version: [2, current]
@description: Create list of _namespace_ steps from a string.
@lot: $in, string $NS = '.', int $dir = 1
@static: 1
@example: |
  $A = Anemon::step('foo.bar.baz');
  $B = Anemon::step('foo/bar/baz', '/');
  $C = Anemon::step('foo.bar.baz', '.', -1);
@result: |
  $A = [
      'foo.bar.baz',
      'foo.bar',
      'foo'
  ];

  $B = [
      'foo/bar/baz',
      'foo/bar',
      'foo'
  ];

  $C = [
      'foo.bar.baz',
      'bar.baz',
      'baz'
  ];
...