---
@version: 2.2
@description: Check the existence of HTML attribute.
@lot:
- $key
@interface:
- /array-access
@example: |
  $div = new HTML('<div id="foo"></div>');

  if (isset($div['id'])) { … }
...
