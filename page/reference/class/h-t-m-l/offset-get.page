---
@version: 2.2
@description: Get HTML attribute.
@lot:
- $key
@interface:
- /array-access
@example: |
  $div = new HTML('<div id="foo"></div>');

  test(
      $div[0], // Return the HTML tag name
      $div[1], // Return the HTML content
      $div[2], // Return the HTML attributes with their value
      $div['id'] // Return the HTML `id` attribute value
  );
...
