---
@version: 2.2
@description: Bulk [file][link:../file] instance from a folder.
@lot:
- string $folder = ROOT, $x = 1, $deep = 0
@static: 1
@example: |
  $files = Files::from('.\path\to\folder');

  foreach ($files as $file) {
      echo $file->name . '<br>';
  }
...
