---
@version: [1, 2, current]
@description: Abort script execution.
@lot: string $message, $exit = true
@static: 1
@example: |
  if (!HTTP::is('post')) {
      Guardian::abort('Method not allowed.');
  }
...