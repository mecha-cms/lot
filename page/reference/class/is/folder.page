---
@version: [2, current]
@description: Check if current input is a valid folder path and make sure it is exists.
@lot:
- $x
@static: 1
@example: |
  if (Is::folder('.\path\to\folder')) { … }
  if (Is::folder('.\path\to\file.txt')) { … }
  if (Is::folder('<a>bcdef</a>')) { … }
  if (Is::folder([1, 2, 3])) { … }
...