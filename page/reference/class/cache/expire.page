---
@version: [2, current]
@description: Check if cache file already expired.
@lot: string $from, $id = null
@static: 1
@example: |
  if (Cache::expire('.\path\to\file.php')) {
      // Expired!
  }
@example-description: Check if cache file of `.\path\to\file.php` already expired.
...