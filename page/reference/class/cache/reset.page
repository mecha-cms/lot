---
@version: [2, current]
@description: Delete cache file.
@lot:
- void
- string $id
- array $id
@static: 1
@example: |
  // Delete all cache(s)
  Cache::reset();
  // Delete single cache
  Cache::reset('foo');
  // Delete multiple cache
  Cache::reset(['foo', 'bar', 'baz']);
...