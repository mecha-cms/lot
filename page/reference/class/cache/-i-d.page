---
title: `$cache::{ID,id}()`
description: Get cache ID.
...

~~~ .php.xmp
Cache::ID($from, $fail = -1);
~~~

~~~ .php.xmp
Cache::id($from, $fail = -1); // alias
~~~

**Example:**

Get cache ID of `path\to\file.php`:

~~~ .php
$id = Cache::ID('path\to\file.php');
~~~

~~~ .php
$hash = md5(File::open('path\to\file.php')->read());
if (is_string($id) && $id !== $hash || $id < File::inspect('path\to\file.php', 'update')) {
    // expired!
}
~~~