---
@version: 2.2
@description: Remove `$_GET` value.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all items
  Get::let();
  // Remove `foo` item
  Get::let('foo');
  // Remove `foo` and `bar` item
  Get::let(['foo', 'bar']);
...
