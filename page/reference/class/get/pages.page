---
@version: [2, current]
@description: Plugged by [page][link:../../extension/page] extension.
@lot: string $folder = PAGE, string $x = 'page', $sort = [-1, 'time'], string $key = null
@return: Anemon
@static: 1
@example: |
  // List all published and archived pages from `.\lot\page\article` folder
  // and sort them by `slug` ascending and output only the `slug` property of each item returned
  $A = Get::pages('.\lot\page\article', 'page,archive', [1, 'slug']);

  // List all published and archived pages from `.\lot\page\article` folder
  // and sort them by `slug` ascending and output only the `path` property of each item returned
  $B = Get::pages('.\lot\page\article', 'page,archive', [1, 'slug'], 'path');
...