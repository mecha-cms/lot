---
@version: [current]
@description: Check current request method.
@lot:
- void
- string $name
- string $name, string $key
@static: 1
@example: |
  // Check if current request method is `POST`
  if (Request::is('Post')) { … }
  if (Request::is() === 'Post') { … }

  // Check if `$_POST['id']` exists
  if (Request::is('Post', 'id')) { … }
...