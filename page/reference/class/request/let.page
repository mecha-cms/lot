---
@version: [current]
@description: Remove `$_REQUEST` value.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all items
  Request::let();
  // Remove `foo` item
  Request::let('foo');
  // Remove `foo` and `bar` item
  Request::let(['foo', 'bar']);
...