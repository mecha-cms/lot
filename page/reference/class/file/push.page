---
title: `$file::push()`
description: Upload a file.
...

~~~ .php.xmp
File::push($name, $path = ROOT, $fn = null);
~~~

**Example:**

~~~ .php
File::push('blob', '.\path\to\folder'); // → for `<input name="blob" type="file">`
~~~

Custom callback function to [sanitize][link:../to/file] the file name:

~~~ .php
File::push('blob', '.\path\to\folder', function($data) {
    $data['name'] = To::file($data['name']);
    return $data;
});
~~~