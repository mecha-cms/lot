---
@version: [2, current]
@description: Remove global variable.
@lot:
- void
- string $key
- array $keys
@static: 1
@example: |
  // Remove all global variable(s)
  Lot::reset();
  // Remove single global variable
  Lot::reset('title');
  // Remove multiple global variable(s)
  Lot::reset(['title', 'content']);
...